import org.apache.tools.ant.filters.*

task copyFiles(type: Copy) {
    from 'src/sample'
    into buildDir

    expand version: gradle.gradleVersion, gr8Tool: 'Gradle'

//    rename 'file.txt', 'file.new'
//    rename '(.*).txt', '$1.new'
    rename { filename -> 'newfile.txt' }
}